---
title: ユーザマニュアル｜外部設定ファイル
description: フォーマット定義ファイル・画面設定ファイルの置き場所と、動作設定（格納先設定シート）の変更方法を説明します
---

# 外部設定ファイル

ナレッジ管理ツールは、フォーマット定義や画面の見た目を **外部のテキストファイル** で管理しています。ファイルを書き換えるだけで、入力項目や画面の見た目を変更できます。保存先フォルダやログなどの動作設定は、各ブック内の **「格納先設定」シート** で管理します（[設定値の変更](../screen_customization_guide_v2.md) を参照）。

このページでは、外部ファイルの種類・置き場所と、設定項目・値を説明します。

## 設定の全体像

| 種類 | 置き場所 | 役割 |
|---|---|---|
| 動作設定（格納先設定シート） | 各ブック内の「格納先設定」シート | 保存先フォルダや動作を決める（[設定値の変更](../screen_customization_guide_v2.md)） |
| フォーマット定義ファイル | `C:\KnowledgeMgr\formats\`（例：`FAULT.txt`） | 型ごとの項目（フィールド）を決める |
| 画面設定ファイル | `C:\KnowledgeMgr\ui\（役）\` | 画面の見た目（列幅・色・項目名）を決める |

!!! warning "文字コードに注意"
    フォーマット定義ファイルと画面設定ファイルは、メモ帳で **「ANSI」(Shift_JIS) 文字コード**・改行コード **CRLF** で保存してください。UTF-8 のまま保存すると、日本語の項目名や値が文字化けします。

## 動作設定（格納先設定シート）

保存先フォルダやログの詳細度などの動作設定は、以前はテキストファイルで持っていましたが、v2.3 からは各ブック内の **「格納先設定」シート** にまとまっています。テキストファイルの作成・編集は不要です。

変更は、管理.xlsm の **M-10「格納先設定」画面** で値を直し、**Ctrl+S** で保存するだけです。設定項目（`data_dir` などの保存先 6 項目と、`debugLevel` / `logRotationRows` / `uiSchemaFailMode` の 3 項目）の意味と値は、[設定値の変更](../screen_customization_guide_v2.md) のページで詳しく説明しています。

## フォーマット定義ファイル

型（フォーマット）ごとの項目を決めるファイルです。`format_dir`（既定では `C:\KnowledgeMgr\formats\`）に置きます。
ファイル名は型の ID に合わせます（例：`FAULT.txt`）。

通常は管理ブックの設計画面から作成・編集しますが、中身は次のようなテキストです。

### 書き方

```text
[FORMAT]
FormatId=FAULT
FormatName=障害対応
===
[FIELD]
FieldName=件名
FieldType=単一行
Required=TRUE
MaxLength=120
fieldPlaceholder=（例）経理システムへログインできない
searchTarget=TRUE
===
[FIELD]
FieldName=カテゴリ
FieldType=選択
Required=FALSE
DropdownOptions=アプリ障害|インフラ障害|ネットワーク障害|セキュリティ
fieldPlaceholder=（例）アプリ障害
searchTarget=FALSE
```

- 先頭の `[FORMAT]` で型そのものの情報を、続く `[FIELD]` で項目を 1 つずつ並べます。
- 項目どうしは `===` の行で区切ります。
- 上から書いた順に、登録画面・検索画面の項目の並び順になります。

### 型そのものの項目（[FORMAT]）

| 設定項目 | 意味 | 値の種類 | 例 |
|---|---|---|---|
| `FormatId` | 型の ID（ファイル名にもなる） | 英数字 | `FAULT` |
| `FormatName` | 画面に表示される型の名前 | 文字列 | `障害対応` |

### 各項目の属性（[FIELD]）

| 設定項目 | 意味 | 値の種類 | 既定の扱い | 例 |
|---|---|---|---|---|
| `FieldName` | 項目名（入力欄に表示される名前） | 文字列 | （必須） | `件名` |
| `FieldType` | 型 | `単一行` / `複数行` / `日付` / `選択` | （必須） | `単一行` |
| `Required` | 入力を必須にするか | `TRUE` / `FALSE` | `FALSE` | `TRUE` |
| `MaxLength` | 入力できる文字数の上限 | 数字（単一行で使用） | 指定なし＝上限なし | `120` |
| `Rows` | 複数行のときの入力欄の高さ | 数字（複数行で使用） | 指定なし＝標準の高さ | `4` |
| `Scroll` | 複数行で欄内スクロールを使うか | `TRUE` / `FALSE` | `FALSE` | `TRUE` |
| `DropdownOptions` | 選択のときの候補 | 候補を縦棒 `\|` で区切る | 指定なし | `低\|中\|高\|緊急` |
| `fieldPlaceholder` | 入力欄に薄く表示する見本 | 文字列 | 指定なし | `（例）山田 太郎` |
| `searchTarget` | 検索で本文として探すか | `TRUE` / `FALSE` | `FALSE` | `TRUE` |

各型の意味や選び方は、[フィールド型の説明](types.md)のページを参照してください。

!!! note "`Scroll` の挙動（2026-06-25 改善）"
    複数行フィールドの `Scroll` を見直しました。

    - **`Scroll=TRUE`**：入力欄をスクロール用の枠（Frame）で包み、**垂直スクロールバーを常時表示**します（内部的には `ScrollBars=2` + `KeepScrollBarsVisible=2`）。欄が空でもバーが表示され、入力に応じて欄が伸び、カーソルへ追従します。
    - **`Scroll=FALSE`**：**スクロールバーを表示しません**（`ScrollBars=0`）。`Rows` で指定した高さの固定欄になります。

    既定は `FALSE` です。

## 画面設定ファイル

画面の見た目（シート名・列幅・色・項目名など）を決めるファイルです。
`ui_dir`（既定では `C:\KnowledgeMgr\ui\`）の、役ごとのフォルダに置きます。

```text
C:\KnowledgeMgr\ui\管理\
C:\KnowledgeMgr\ui\検索\
```

各ファイルの設定項目の詳しい一覧は、[画面設定ファイル一覧](../stanza/index.md)のページにまとめています。

## 関連ページ

- [フィールド型の説明](types.md)
- [フォーマット設計の流れ](format.md)
- [セットアップ](setup.md)
- [画面設定ファイル一覧](../stanza/index.md)
